var numero = prompt('Adicione um numero')

function primeNumber (numero){ var numero = 0; for (var i = 0; i <= numero; i++){ 
    if ((numero % 1 === 0) && (num % numero === 0)){ return (primeNumber(i)); } } }